export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-3xl font-bold mb-4">Bem-vindo ao seu Dashboard!</h1>
      <p className="text-text-muted">
        Navegue pelos módulos na barra lateral para começar a usar o Facillit Hub.
      </p>
      {/* Aqui virão os componentes principais do dashboard, como cards de resumo, etc. */}
    </div>
  );
}